import os
import re

path = 'export PATH="$PATH:"/usr/bin/burr-main/""'
pattern = re.compile(path)

folder_dir = os.path.expanduser('~')
bashrc = os.path.abspath('%s/.bashrc' % homefolder)

def add_bashrc():
  with open(bashrc, 'r') as f:
    lines = f.readlines()
    for line in lines:
      if pattern.match(line):
        return
    out = open(bashrc, 'a')
    out.write('\n%s\n%s' %(path,alias ))
    out.close()

if __name__ == "__main__":
  add_bashrc()
  print("Done.")
